# git --version
# git config --global user.name "name"
# git config --global user.email "email addy"
# git config --list

git init
git add <file>
git status
git commit





CD into the directory where are your local repos are

*git clone https://github.com/robertrodriguezjr70/devops-master-class.git

git add .

git commit -m  "comments here"

git push origin

